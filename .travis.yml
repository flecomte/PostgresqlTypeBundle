language: php

php:
  - 5.4
  - 5.5
  - hhvm

before_script:
  - composer install -n
  - mkdir -p build/logs

script:
  - phpunit --coverage-text --coverage-clover build/logs/clover.xml

after_script:
  - php vendor/bin/coveralls -v
